import { VERIDA_ONE_CONTEXT_NAME } from 'features/veridaOne'

import { VERIDA_VAULT_CONTEXT_NAME } from 'constants/application'

export const IDENTITY_MIGRATION_USE_PREDEFINED_CONTEXTS = true

export const IDENTITY_MIGRATION_PREDEFINED_CONTEXT_NAMES = [
  VERIDA_VAULT_CONTEXT_NAME,
  VERIDA_ONE_CONTEXT_NAME,
  'Verida Missions',
  'Verida: Markdown Notes Demo',
]
