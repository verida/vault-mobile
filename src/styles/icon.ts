import { StyleSheet } from 'react-native'

const iconStyle = StyleSheet.create({
  normal: {
    width: 48,
    height: 48,
  },
  large: {
    width: 80,
    height: 80,
  },
})

export default iconStyle
