import React from 'react';
import { Container, Content, Text } from 'native-base';

export default ListView = ({  }) => {
    return (
        <Container>
            <Content>
                <Text>List view</Text>
            </Content>
        </Container>
    );
};